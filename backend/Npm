# Sempre que houver (criação/alteração, campos, enums, relações)
npx prisma migrate dev --name minha_migration

# Garantir se tiver tudo ok no Prisma Client
npx prisma generate

NPM's usados
npm install @nestjs/jwt passport passport-jwt
npm install @nestjs/passport passport-google-oauth20
npm install bcrypt
npm install class-validator class-transformer
npm install class-validator@0.14.0
npm install @nestjs/mapped-types
npm install @prisma/adapter-pg pg
npm install @nestjs/config
npm install -D @types/bcrypt
npm install @nestjs/schedule
npm install @types/node --save-dev

# npx prisma migrate dev --name add_updated_at: Subir alguma mudança que foi feita no Prisma Schema

/users/dto/create-user.dto.ts:
name?, password? - Já que terá duas formas de login (Cadastro Normal) e (Cadastro via Google)

prisma.schema: role ROLE: A permissão que o usuário terá dentro do sistema, podendo ser 'USER' ou 'ADMIN'

HttpException + HttpStatus: Definir o Status Correto da Resposta e NestJS entende que é um ERRO HTTP


npm install passport-google-oauth20
npm install -D @types/passport-google-oauth20